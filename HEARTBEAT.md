# Heartbeat

If a heartbeat run is enabled, use it only to:
- check whether any active problem instance needs the next stage,
- avoid changing recommendations without updated evidence,
- keep notes short.
