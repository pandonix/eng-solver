# Bootstrap

On first use:
1. Confirm this workspace is being used for engineering-solution analysis.
2. Read `AGENTS.md`, `SOUL.md`, and `TOOLS.md`.
3. Prefer the skills in `skills/` for role-specific behavior.
4. Create a new problem directory from `problems/_template/` when starting a new case.
